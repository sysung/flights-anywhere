# Video Walkthrough

Add the walkthrough link here before submission.

Suggested demo flow:

1. Start the API with Docker.
2. Call `GET /healthz`.
3. Call `POST /api/flights/search` without destination.
4. Call `POST /api/flights/search` with `destination: "LAX"`.
5. Show session/log behavior in the terminal.
6. Run `python3 -m unittest discover -v`.

# Runtime API dependencies
fastapi>=0.109.2
uvicorn[standard]>=0.27.1
pydantic>=2.6.1
pydantic-settings>=2.1.0
httpx>=0.27.0

# Browser automation. After pip install, install the Chromium browser binary with:
#   python3 -m playwright install chromium
# Dockerfile runs this automatically during image build.
playwright>=1.60.0

# Test dependencies
pytest>=8.0.2

# Legacy/original project dependencies kept for compatibility with existing docs/code.
sqlalchemy>=2.0.27
psycopg2-binary>=2.9.9
google-generativeai>=0.4.1
apscheduler>=3.10.4
tzdata
bbpb
confluent-kafka>=2.3.0
alembic
